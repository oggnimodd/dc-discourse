{
	"name": "discourse-lab",
	"version": "1.0.0",
	"scripts": {
		"setup": "./launcher bootstrap app",
		"start": "./launcher start app --docker-args '--cpus=0.8 --memory=2g'",
		"stop": "./launcher stop app",
		"logs": "./launcher logs app",
		"nuke": "./launcher destroy app",
		"rebuild": "./launcher rebuild app",
		"setup:admin": "./launcher enter app rake admin:create"
	}
}
